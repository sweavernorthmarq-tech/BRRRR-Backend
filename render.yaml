services:
  - type: web
    name: brrrr-deal-analyzer
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        scope: build,runtime
        sync: false
